@model IEnumerable<GestionInventario.Models.Privilegio>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.privilegio1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.privilegio1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idPrivilegio }) |
            @Html.ActionLink("Details", "Details", new { id=item.idPrivilegio }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idPrivilegio })
        </td>
    </tr>
}

</table>
